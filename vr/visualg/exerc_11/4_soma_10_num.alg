Algoritmo "Soma_dez"
//Receber 10 valores e efetuar a soma dos valores recebidos. 

var
a, x, i:inteiro

Inicio
    enquanto(i < 5)faca
    escreval("digite um numero: ")
    leia(a)
    x <- x + a
    i <- i + 1
    fimenquanto
escreval(x)
fimalgoritmo
